# Job Application Tracker (CLI)

A simple command-line tool to track job applications.  Add, View, and Persist Data between sessions.

## Usage
Run 'Python3 tracker.py' and follow the menu prompts.

## Built with
Python (json module for data persistence)